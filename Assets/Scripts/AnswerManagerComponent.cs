using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//handles the player answers
public class AnswerManagerComponent : MonoBehaviour
{
    public bool playerChoseAnswer = false;
    public GameObject numberQuestionFrame;
    public bool shouldDisplayNumberQuestion = false;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
